import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';
import federation from '@originjs/vite-plugin-federation';

export default defineConfig({
  plugins: [
    react(),
    federation({
      name: '<%= name %>',
      remotes: {
        // Modules will be loaded dynamically from the registry
      },
      shared: ['react', 'react-dom', 'react-router-dom']
    })
  ],
  build: {
    modulePreload: false,
    target: 'esnext',
    minify: false,
    cssCodeSplit: false
  },
  server: {
    port: <%= port %>,
    strictPort: true,
    cors: true
  },
  preview: {
    port: <%= port %>,
    strictPort: true,
    cors: true
  }
});
